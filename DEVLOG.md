# TreeNav 개발 일지

## 2026-09-05 — Phase 1 · 2 완료, 중첩/정렬 확장, 저장소 개설

### 오늘 도달한 상태

Phase 1과 Phase 2가 모두 동작 확인되었고, 지시서에 없던 확장(노트 중첩, 수동 정렬,
자동 평탄화)까지 들어갔다. 저장소를 만들고 GPL-3.0으로 공개 준비를 시작했다.

**동작 확인된 것**

- TreeNav 사이드바 뷰, 리본 아이콘, `Open TreeNav` 커맨드
- 폴더 펼침/접기 + 재시작 후 복원, 활성 노트 표시
- 파일/폴더 Drag & Drop, 잘못된 이동 사전 차단
- 인라인 이름 변경(F2), 새 노트/폴더 생성 후 즉시 이름 편집
- Folder Note (`Folder/Folder.md`), 목록에서 숨김, **양방향 이름 동기화**
- 외부(탐색기 등) 변경 자동 반영
- 아이콘 / 색 / 굵기 / 기울임 / 크기 지정, 경로 이동·이름 변경 시 유지
- 키보드 네비게이션 (`↑↓←→`, `Enter`, `F2`, `Delete`, `Ctrl/⌘+N`, `Ctrl/⌘+Shift+N`)
- 컨텍스트 메뉴, `Move to…` 폴더 검색 이동
- **노트를 노트 아이콘에 떨궈 하위로 중첩**, 이름 위/아래에 떨궈 순서 지정
- **폴더 내 수동 순서 저장**, 마지막 자식이 빠지면 **자동 평탄화**

### 설계에서 붙잡고 있는 원칙

**1. 별도 트리 모델을 두지 않는다.** `TFolder.children`을 직접 읽는다. 중복 상태가
없으니 외부 변경과 어긋날 여지가 구조적으로 사라진다.

**2. 경로가 유일한 키다.** Obsidian은 안정적인 파일 ID를 제공하지 않는다. 그래서
펼침 상태 · 스타일 · 수동 순번 · 중첩으로 만든 폴더 목록을 전부 경로로 키잉하고,
`rename` 이벤트마다 **하나의 prefix 재작성 패스**로 함께 이관한다. 폴더 rename은
자식마다 이벤트가 오지 않으므로 이 방식이 유일한 실용해다.

**3. Folder Note 규칙을 재사용해 개념을 늘리지 않는다.** 중첩은 새로운 개념이
아니라 `A.md` → `A/A.md` 변환일 뿐이다. 그래서 중첩된 노트의 이름 변경 · 이동 ·
스타일 유지가 전부 기존 로직으로 처리된다.

**4. 행이 자리를 물려받으면 상태도 물려받는다.** 중첩하면 폴더 행이 노트 행의
자리를 차지하므로 폴더가 노트의 순번·스타일을 승계하고, 평탄화하면 반대로
노트가 폴더의 것을 승계한다. 이 대칭이 깨지면 위치가 튀고 스타일이 사라진다.
(실제로 두 방향 모두 버그로 한 번씩 겪었다.)

**5. vault 변경은 전부 공식 API로.** `fileManager.renameFile`(링크 갱신 포함),
`fileManager.trashFile`(사용자 휴지통 설정 존중). filesystem 직접 조작 없음.

### 겪은 버그와 근본 원인 (재발 방지용)

**뷰 전체가 빈 화면** — `TreeNavView`에 추가한 `open()` 메서드가 Obsidian `View`의
**내부 `open()`** 을 덮어썼다. 이 메서드는 `obsidian.d.ts`에 없어서 TypeScript가
잡지 못하는데, 런타임에서 `WorkspaceLeaf`가 뷰를 마운트할 때 호출한다. 뷰가
마운트 단계에서 죽어 `onOpen`이 아예 실행되지 않았다.
→ 액션 메서드를 `openItem` / `renameItem` 등으로 개명. 스모크 테스트에
**예약어 가드**를 넣어 재발 시 빌드가 실패하도록 했다.

**폴더 rename 시 폴더 노트가 따라오지 않음** — rename 이벤트 시점에 자식 파일의
경로가 아직 갱신되지 않아 경로 조회가 `null`을 반환했다.
→ 경로 문자열 대신 `folder.children`에서 객체로 찾는다. 경로 갱신 타이밍과 무관해진다.

**중첩 후 항목이 맨 아래로 이동** — 경로가 `A.md` → `A`로 바뀌어 새 폴더가 순번 없는
항목이 됐다. 스타일도 같이 사라졌다. → 위 원칙 4.

**평탄화 후 스타일·위치 소실** — 승계를 한 방향으로만 구현했다. → 위 원칙 4.

**진단 과정 자체의 실패** — 증상 보고를 받고 코드를 눈으로 좇으며 추측을 반복했다.
검증 수단부터 만들었어야 했다. 그래서 스모크 테스트를 만들었고, 이후 버그들은
전부 테스트가 먼저 잡았다.

### 검증 수단

`scripts/smoke-test.mjs` — jsdom 위에서 **빌드된 `main.js`를 실제로 로드·렌더링**한다.
Obsidian API는 stub이고, 가짜 vault는 변경 가능하며 `rename` / `create` / `delete`
이벤트를 실제처럼 발생시킨다(경로 이관 로직이 여기에 의존하므로 필수).

검증 항목: 렌더링 · 폴더 노트 숨김 · 수동 순서 · 스타일 적용 · 아이콘 통일 ·
자식 없는 폴더의 화살표 · 중첩 · 평탄화 · 사용자 폴더 보호 · 예약어 가드.

`npm run build`가 `타입체크 → 번들 → 스모크 테스트` 순으로 돌기 때문에 하나라도
실패하면 배포 파일이 나가지 않는다.

---

## 2026-09-05 (2) — 위치 이동, 커맨드, 아이콘/폰트/트리 스타일

### 추가된 것

- **아웃라인 이동** — `Shift+↑↓` 한 칸, `Shift+Home/End` 계층 끝, `Shift+←→` 들여쓰기/내어쓰기.
  우클릭 메뉴에도 6개 항목. Indent는 위 형제 노트에 대한 중첩이고 Outdent는
  "이전 부모 뒤에 놓기"라, 드래그가 쓰던 연산을 그대로 재사용한다.
  순서·중첩 로직은 `OutlineService`로 모아 드래그와 키보드가 같은 코드를 지난다.
- **커맨드 13개** — 기본 단축키 없이 등록. 사용자가 Obsidian 단축키 설정에서
  직접 배정한다. 모두 트리가 키보드 포커스를 가질 때만 발동한다.
- **이모지 아이콘** — Lucide와 함께 쓸 수 있다. 검색창에 직접 붙여넣어도 되고,
  이름이 붙은 43개는 검색된다. 단일 코드포인트에는 `U+FE0F`를 붙여 컬러 표현을 강제한다.
- **글꼴 종류** — 테마 변수 토큰(`interface`/`text`/`monospace`)으로 저장.
  폰트 이름을 직접 저장하지 않아 다른 기기에서도 깨지지 않는다.
- **클래식 트리 스타일** — 점선 연결선과 ⊞/⊟ 박스. 컨테이너 클래스 하나로 전환하므로
  재렌더링이 필요 없다.

### 이번에 잡은 버그

**단축키가 전혀 먹지 않음** — 행을 클릭하면 노트가 열리면서 포커스가 편집기로
넘어갔다. 커맨드는 "트리가 포커스를 가질 때만" 발동하도록 게이트가 걸려 있으니
항상 거짓이었다. VS Code 방식으로 고쳤다 — **클릭은 `active: false`로 열어 포커스를
트리에 남기고, `Enter`와 메뉴의 Open만 편집기로 포커스를 넘긴다.** 덕분에 화살표
네비게이션도 클릭 직후 바로 이어진다.

### 아이콘 규칙 정리

"폴더인가"가 아니라 **"원래 노트였는가"** 로 판단한다. 직접 만든 폴더는
`folder-closed`, 중첩으로 생긴 폴더는 노트 아이콘. 개별 지정 아이콘은 언제나 우선.

### 이후 다듬은 것

- **글꼴 자유 입력** — 처음엔 테마 토큰만 허용했는데, 그건 기술적 제약이 아니라
  안전 기본값에 대한 판단이었다. 토큰과 자유 입력을 상호 배타적으로 둘 다 제공한다.
  플랫폼마다 폰트가 다르므로 입력란이 **쉼표 폴백 스택**을 안내한다.
  (Obsidian 공개 API에는 폰트 관련 항목이 전혀 없어 폰트 목록 제공은 불가능.)
- **클래식 연결선은 실선 1px로 확정** — 처음엔 `border: dotted`였는데, 브라우저가
  선 길이에 맞춰 점 간격을 조정하는 탓에 가로·세로 밀도가 달라 보였다. 고정 주기
  gradient로 바꿔 밀도는 맞췄지만, 배율 100%가 아닌 화면에서는 1px 점이 분수 픽셀에
  걸려 여전히 흔들렸다. 실선이 확실히 깔끔해 **점선 옵션은 제거**했다.
  (`classic-solid` 값은 `classic`으로 이관.)
- 클래식 모드 행 높이 21px, 세로 여백 제거.

### 이번에 배운 것 (도구 관련)

**bash heredoc은 백슬래시를 삼킨다.** `️`, CSS `2` 같은 이스케이프가
전송 중 손상돼 잘못된 제어문자가 파일에 들어갔다. 실제로 두 번 발생했다.
→ 이스케이프가 들어가는 내용은 Write/Edit 도구를 쓰거나, 유니코드 문자를
직접 넣는다. CSS는 제어문자 검사를 빌드에 넣어 두면 좋다.

## 2026-09-05 (3) — Appearance 통합 다이얼로그

### 글꼴 자유 입력 제거

한 세션 전에 넣은 기능을 다시 벘다. 입력란으로 받는 방식 자체가 좋지 않았고
(설치된 폰트를 모르는 채 이름을 타이핑하라는 요구), 중요도도 높지 않다는 판단.
타입페이스는 다시 **테마 토큰 3종**만 남기고, `FontFamily`는 `FontToken`으로 좀혔다.
이전 버전이 저장한 폰트 이름은 **로드 시 제거**한다 — 어느 대화상자도 고칠 수 없는
모양을 남겨두는 것보다 테마 기본값으로 돌리는 편이 정직하다.

(나중에 다시 하게 된다면: Electron의 `window.queryLocalFonts()`로 설치 폰트
목록을 뽑을 수 있다. Obsidian 자체 모양 설정이 그 방식이다. 모바일엔 없는
API라 폴백이 필요하고, 반환값은 PostScript 이름 전수라 패밀리로 묶어야 한다.)

### Set color + Set font → Appearance

메뉴가 분리돼 있어 한 항목을 꾸미려면 대화상자를 두번 열어야 했고, 색과 글꼴이
합쳐졌을 때 어떻게 보이는지 확인할 방법이 없었다. 하나로 합치고 UI를 다시 잡았다.

- 상단에 **라이브 프리뷰** — 실제 트리 행 마크업(`.treenav-item-self`)을 그대로 썬다.
  대화상자가 보여주는 것과 트리가 그리는 것이 어긋나려야 어긋나질 수 없다.
- 굵기/기울기는 드롭다운 2개 대신 **B / I 토글 버튼**.
- 색은 스와치 10개 + 커스텀 + 초기화. 골랐다고 닫히지 않는다(이전 ColorModal은 닫혔다).
- **Apply 전에는 아무것도 저장되지 않는다.** Reset all은 아이콘 포함 전체 초기화.
- 메뉴는 `Set icon`(빠른 경로) + `Font & color` 두 개. 커맨드 `set-color`/`set-font`는
  `set-appearance` 하나로 합쳐졌다 — **그 둘에 단축키를 배정했다면 다시 지정해야 한다.**

### 스모크 하네스가 대화상자까지 다룬다

지금까지 `Setting` 스텅은 콜백만 삼키는 빈 꺼를 건네서, 대화상자는 한 번도 실행된 적이
없었다. 실제 엘리먼트를 만드는 `Setting`/`ButtonComponent`/값 컴포넌트로 바꾸고,
`Modal.open()`이 `onOpen()`을 실제로 호출하게 했다. 그 결과:

- Appearance 대화상자를 열어 B 누르기 → 프리뷰 반응 확인 → 스와치 클릭 → Apply →
  저장값과 **트리 행까지** 검증하는 테스트가 생겼다.
- 설정 탭도 `display()`를 실제로 돌려 본다.
- 가짜 워크스페이스의 `getLeavesOfType`가 빈 배열을 돌려줘서 플러그인의 팬아웃(
  `refreshStyles` 등)이 테스트 뷰에 닿지 않고 있었다. 이제 열린 leaf를 제대로 보고한다.

## 2026-09-05 (4) — 다중 선택과 멀티 드래그

### 선택 모델

`selectedPath: string | null` → `Set<string>` + `lead` + `anchor` 세 가지로 나눴다.

- **lead** — 단일 항목 명령(이름 변경, 위치 이동 등)이 대상으로 삼는 행. 마지막으로 만진 행.
- **anchor** — Shift+클릭이 범위를 재는 기준점. Shift+클릭은 anchor를 옮기지 않아서
  같은 기준으로 범위를 넓혔다 줄였다 할 수 있다.

행이 다시 그려질 때 `registerItem`이 선택 상태를 경로로 복원한다. 선택은 저장하지 않는다.

### 제스처 충돌 하나

`Ctrl/⌘+클릭`은 원래 **새 탭에서 열기**였다. 코어 파일 탐색기와 마찬가지로 다중 선택에
내줬고, 잃어버린 기능은 **가운데 클릭**으로 옮겼다. 우클릭 메뉴의 "Open in new tab"은 그대로다.

`Shift+화살표`는 **선택 확장이 아니라 항목 이동**으로 남겼다. 일반적인 트리와 다르지만
이 트리는 재배치할 수 있는 아웃라인이고, 그쪽이 더 자주 쓰인다는 판단. 범위 선택은
Shift+클릭으로 한다.

### 부분적으로만 유효한 드롭

여러 개를 끌 때 대상이 그 중 일부에만 맞는 경우(이미 그 폴더에 있는 항목이 섞여 있다든지)가
흔하다. **하나라도 맞으면 드롭을 허용하고, 맞지 않는 것은 제자리에 둔다.** 전부 유효할
때만 허용하면 흔한 상황에서 아무것도 안 되고, 그게 더 나쁘다.

이동은 **하나씩 순차 처리**한다. 동시에 보내면 같은 이름을 두고 경쟁한다.
"뒤에 놓기"는 역순으로 넣어야 원래 순서가 유지된다(`placeAllNextTo`).

### 여러 개일 때의 메뉴

이름 변경 · 폴더 노트 · 아웃라인 이동은 한 행에 대한 것이라 **빼버렸다**. 첫 번째 항목에
슬쩍 적용하는 것보다 없는 편이 낫다. 남긴 것: `Move to…`, `Font & color`(첫 행으로
미리보기, 전체에 적용), `Delete N items`.

**여러 개 삭제는 설정과 무관하게 항상 묻는다.** "삭제 확인 안 함"은 한 번에 한 개를
전제로 동의한 것이고, 되돌리기 부담이 다르다.

### 하네스

드래그도 실제 리스너를 통해 검증한다. jsdom은 레이아웃을 계산하지 않으므로
`getBoundingClientRect`를 행마다 지정해 드롭 존을 만들어 준다. Ctrl+클릭으로 두 개를
고르고 → 하나를 끌어 폴더에 떨어뜨리고 → **둘 다 옮겨졌는지**, 선택되지 않은 행을 끌면
**그 행만** 움직이는지까지 확인한다.

## 2026-09-05 (5) — 모바일: HTML5 DnD를 버리고 포인터 이벤트로

### 왜 안 됐나

HTML5 드래그앤드롭(`draggable`, `dragstart`/`dragover`/`drop`)은 **마우스 전용 프로토콜**이다.
터치는 `dragstart`를 아예 만들지 않는다 (Android WebView 전혀, iOS는 iPad 일부).
그래서 폰에서는 트리 재배치가 불가능했다.

Obsidian 공개 API(`obsidian.d.ts`)에 드래그 관련 항목은 **하나도 없다** — 확인함.
코어 탐색기가 모바일에서 무엇을 하든 그건 내부 구현이고 우리가 부를 수 있는 게 아니다.
따라서 직접 구현하는 것 외에 선택지가 없었다.

### 포인터 이벤트로 재작성

마우스·펜·터치가 한 경로로 처리된다. 드래그 시작 방식만 다르다:

- **마우스** — 4px 이동하면 드래그
- **터치** — 400ms 길게 누르면 집어 든다. 그 전에 움직이면 **스크롤**로 판단(10px 슬롭)

길게 누른 뒤 **움직이면 드래그, 그대로 놓으면 컨텍스트 메뉴.** 플랫폼들이 쓰는 바로 그
관용구라 길게 누르기 = 메뉴 와 충돌하지 않는다. 길게 누르는 동안 브라우저 기본
컨텍스트 메뉴는 막고(우리가 직접 띄운다), 드래그 중에는 `touchmove`를 non-passive로
받아 `preventDefault()`로 스크롤을 막는다.

HTML5가 공짜로 주던 것들을 직접 만들어야 했다:

- **드래그 고스트** — 포인터 옆을 따라다니는 미리보기. 여러 개일 때 개수 배지가 붙는다
  (HTML5 드래그 이미지보다 오히려 낫다).
- **가장자리 자동 스크롤** — 화면이 좁은 모바일에서는 이게 없으면 먼 곳으로 못 옮긴다.
- **드롭 대상 판정** — `document.elementFromPoint` → `.closest(".treenav-item-self")`.
- **클릭 삼키기** — pointerup 뒤에 따라오는 click이 방금 옮긴 노트를 열어버리지 않도록.

존 판정(into/nest/before/after)과 실행부는 그대로 재사용했다. 바뀐 건 입력 계층뿐이다.

### 잃은 것

에디터로 드래그해 경로 텍스트를 떨어뜨리는 것. HTML5 DnD에 딸려오던 부수 효과였고,
제대로 된 링크도 아니어서 아쉽지 않다.

### 겹지기는 있는데 펼치기가 없었다

헤더 버튼을 **하나로 토글**하게 했다. 둘 다 두면 항상 하나는 쓸모가 없고,
같은 질문을 반대쪽에서 물을 뿐이다. 열린 게 있으면 `chevrons-down-up`(접기),
없으면 `chevrons-up-down`(펼치기). 커맨드 `toggle-fold`로도 쓸 수 있다.

펼치기는 모든 행을 그리므로 수천 개 볼트에서는 무거울 수 있다 — 아래 "대용량 성능"
측정할 때 같이 본다.

### 모바일 치수

`.is-mobile`에서 행 높이 24 → 34px, 들여쓰기 17 → 20px. 손가락은 마우스보다 넓다.

### 그 김에 고친 것

- **Esc 취소** — HTML5에서는 브라우저가 공짜로 해주던 것. 직접 달았다.
- **팝아웃 창** — Obsidian은 탭을 별도 창으로 떼어낼 수 있고, 그 창은 **다른
  `document`**다. 전역 `document`/`window`를 쓰면 히트 판정과 고스트가 엉뚝한 창을
  본다. 제스처가 시작된 행의 `ownerDocument` 기준으로 바꿨다. HTML5는 리스너가
  엘리먼트에 붙어 있어 신경 쓸 필요가 없던 부분이다.

### 데스크톱에서 잃는 것

- **트리 밖으로 끌기** — 에디터나 다른 앱으로의 드롭. 하던 일은 `text/plain`
  경로 문자열 삽입이라 제대로 된 링크도 아니었다.
- **OS 드래그 커서**(`dropEffect`)와 반투명 드래그 이미지.

반면 **반응은 빨라진다.** `dragover`는 브라우저가 솔라내지만 `pointermove`는
그대로 온다. 시작 임계값도 우리가 정한 4px이다.

### 하네스

터치 제스처 3종을 실제 이벤트로 검증한다: 바로 움직이면 아무것도 안 집히고(스크롤),
길게 누른 뒤 움직이면 옮겨지고, 길게 눌렀다 그대로 놓으면 메뉴가 뜬다. Menu 스텁이
항목 제목을 기록하도록 바꿔서 "그 행의 메뉴가 맞는지"까지 확인한다.

## 2026-09-05 (6) — 우클릭 메뉴 정리

### 내가 틀렸던 진단

메뉴에 로컬 그래프·백링크·나가는 링크·파일 속성·개요가 끈어 붙는 것을
"코어 플러그인이 붙이는 것이라 어쩔 수 없다"고 설명했다. **틀렸다.** 사용자가
"코어 탐색기에서는 안 보이는데"라고 반박해서 `obsidian.asar`를 직접 뒤졌고,
원인은 **우리가 넘기는 인자**였다.

```js
// 코어 파일 탐색기
trigger("file-menu", menu, file, "file-explorer-context-menu", null)
//                                                             ^ leaf 없음
// TreeNav (이전)
trigger("file-menu", menu, file, "treenav", this.leaf)
```

그 다섯 항목의 조건은 `file instanceof TFile && leaf && source !== "sidebar-context-menu"`다.
**leaf가 있어야** 붙는다 — "이 창을 분할해 옆에 열기" 계열이라 당연하다.

반대로 **버전 내역 열기**는 source를 검사한다:
`("tab-header"|"more-options"|"file-explorer-context-menu") === source`. 우리가 `"treenav"`를
넘겨서 **받지 못하고 있었다.**

→ source를 코어 탐색기와 동일하게, leaf는 `null`로. 이 뷰는 실제로 파일 탐색기고,
플러그인들은 그 문자열로 판단한다. 덤으로 폴더 우클릭 시 "폴더에서 검색"도 생긴다.

### 순서

**코어 항목 → 구분선 → 우리 항목.** 트리거를 먼저 부른다.
우리 항목에는 **`setSection()`을 쓰지 않는다** — 섬션을 주면 코어 그룹 안으로
섮여 들어가서 따로 모이지 않는다. (`setSection`은 구분선을 그리는 명령이 아니라
그룹 이름표다. 메뉴가 그룹 사이에 구분선을 그을 뿐.)

우리 블록 내부 순서: **Open → Rename·Set icon·Font & color → New note·folder →
아웃라인 이동 6 → Delete.** 자주 쓰는 것을 앞으로, 되돌리기 비용이 가장 큰 것을
맨 뒤에 혼자. Rename은 11번째 → 3번째.

### 순서가 코어와 달랐던 이유

코어 항목이 들어오긴 하는데 **배열 순서가 코어 탐색기와 달랐다.** Obsidian 메뉴는
항목을 섬션별로 정렬하는데, **그 순서를 메뉴에 미리 알려줘야** 한다. 안 알려주면
각 섬션의 첫 항목이 도착한 순서대로 배치된다.

코어 파일 탐색기가 쓰는 목록(asar에서 확인):

```js
addSections(["title","open","action-primary","action","info","view","system","","danger"])
```

같은 목록을 쓴다. 빈 문자열 `""`이 **섬션 없는 항목들의 자리**라, 우리 블록이
통째로 코어 그룹들 뒤에 모인다. 우리 항목에 섬션을 주지 않는 이유가 여기서도 맞는다.

`addSections`는 **공개 타이핑에 없다.** 있을 때만 부르도록 `addSections?.()`로 둘렀다 —
없어지면 예전의 도착 순서로 돌아갈 뿐 깨지지 않는다. (동일한 이름으로
런타임 멤버를 *가리는* 것과는 다른 종류의 위험이다.)

### 중복 제거

우리 `Move to…`를 단일 메뉴에서 뺀다. 코어의 "파일을 다음 위치로 이동"과 같은
기능이고, 그건 `"link-context-menu"`만 빼면 어디서든 붙는다. **다중 선택 메뉴에는
남긴다** — 코어는 우리 선택 개념을 모르고 한 개만 옮긴다.

### "복사본 생성"은 받을 수 없는 항목이다

코어에 있는데 안 따라오는 게 이상하다는 지적을 받고 asar을 다시 봤다.
바이트 오프셋이 순서를 증명한다:

```
3,203,803   i.addItem(... menuOptMakeCopy() ...)          ← 탐색기가 직접 넣고
3,204,402   trigger("file-menu", i, t, ..., null)          ← 그다음 이벤트
```

번들 전체에서 `menuOptMakeCopy`는 이 한 곳뿐이다. 새 탭에서 열기·우측 화면에서 열기도
같은 블록. **이벤트를 듣는 쪽은 구조적으로 받을 수 없다** — 물려받는 게 아니라
다시 만드는 수밖에 없다.

`Vault.copy(file, newPath)`로 구현. 공개 API이지만 **1.8.7 이상**이라
`minAppVersion`을 1.5.0 → 1.8.7로 올렸다. 어차피 1.5.0은 근거 없이 적힌 값이었고,
이제는 **검증된 하한**이다. 폴더는 코어를 따라 제외했다(코어도 파일만 제공).

### 다음에 볼 것: `files-menu`

조사 중 발견. 여러 파일용 이벤트가 따로 있고 **공개 API다**:

```ts
on("files-menu", (menu, files: TAbstractFile[], source, leaf) => ...)
```

코어 탐색기도 다중 선택 우클릭에서 이걸 쓴다. 지금 우리 다중 선택 메뉴는
아무것도 쓰지 않아 코어의 "여러 파일 이동·병합·삭제"를 받지 못한다.

### 서브메뉴는 쓸 수 없다

코어 메뉴의 "경로 복사 ›"는 서브메뉴지만, `MenuItem`에 `setSubmenu`는 공개
API에 **없다**. 런타임엔 있겠지만 문서화되지 않은 멤버를 쓰다 데인 전과가 있어
손대지 않았다. 아웃라인 6개를 접려면 개수를 줄이는 수밖에 없는데, 사용자가
편리하다고 하여 그대로 둔다.

## 2026-09-05 (7) — 사용성 묶음

### 다중 선택 메뉴에 `files-menu`

단일 메뉴에 한 것과 동일. 코어의 "여러 파일 이동·병합·삭제"가 들어온다.
`file-menu`와 별도 이벤트고 공개 API다.

### 폴더 더블클릭 = 접기

첫 클릭이 이미 펼치고 폴더 노트를 열어버리므로 실질적으로 **접기 전용**이다.
더블클릭 판정을 기다리면(~250ms) 모든 클릭이 느려지므로 그쪽이 더 나쁜 거래.
작은 화살표를 조준하지 않아도 된다는 게 생각보다 편하다.

### 이모지 31개 추가 (43 → 74)

사람·표정·손·작업·시간·장소. 목록은 **이름으로 검색되는 것**의 사전일 뿐이고,
목록에 없는 이모지도 붙여넣기로 쓸 수 있다.

### 아이콘 선택기 다단

`repeat(auto-fill, minmax(200px, 1fr))` — 모달 폭에 따라 **2열 또는 3열**로 알아서
바뀜다. 열 수를 고정하면 좁은 창에서 이름이 잘린다.

단점: 위/아래 화살표가 DOM 순서(→ 다음, 줄 끝에서 아래)로 움직여 격자 감각과
어깋난다. Obsidian 서제스트가 좌우 화살표를 쓰지 않아 잡을 수 없다.

### 형광펜 배경색

행 전체가 아니라 **이름 텍스트에만** 칠한다. 행 배경에 칠하면 hover·선택·활성 노트
하이라이트와 매번 싸운다.

구현 요점: 제목 엘리먼트는 `flex: 1 1 auto`라 행 끝까지 득어있다. 그대로 배경을 칠하면
형광펜이 아니라 **막대**가 된다. 하이라이트가 있을 때만 `flex: 0 1 auto`로 바꿔
글자 폭에 맞추고, 줄임표는 그대로 동작한다. DOM 변경 없음(인라인 리네임이
제목 엘리먼트를 통째로 갈아끼우므로 중요했다). 편집 중에는 다시 전체 폭.

색은 6개, **반투명 rgba**로 고정. 테마 토큰은 불투명이라 글자를 죽이고,
반투명이라 라이트·다크 양쪽에서 같은 값이 통한다.

## 2026-09-05 (8) — 활성 노트 찾아가기

기본값은 **꺼짐**이다. 매번 따라가게 하면 경로상의 모든 상위 폴더가 의사와 무관하게
펼쳐지고, 링크를 타고 다니면 트리가 계속 요동한다. 대신 **언제든 부를 수 있는 커맨드**를
준다 — 필요한 사람은 설정을 켜면 된다.

커맨드는 **트리 포커스 게이트를 쓰지 않는다.** 다른 트리 명령들은 트리에 포커스가
있을 때만 발동하지만, 이건 **에디터에서 부르는 것이 전부**라 그 규칙을 적용하면
쓸모가 없다. 뷰가 없으면 열어서 보여준다.

숨겨진 폴더 노트는 자기 행이 없으므로 **폴더 행을** 보여준다. 어차피 활성 표시를
달고 있는 게 그 행이다.

## 2026-09-05 (9) — 대용량 성능 측정

`npm run bench` — 하네스에 합성 볼트(24 areas / 4 projects / 3 parts)를 넣고 잰다.
`npm run make-vault`은 같은 모양을 **디스크에** 써서 실제 Obsidian에서 열어볼 수 있게 한다.

| | 1,000 | 5,000 | 20,000 |
|---|---|---|---|
| 접힌 상태 렌더 | 18.7 ms | **11.0 ms** | **16.2 ms** |
| 전체 펼치기 | 312 ms | 828 ms | 6,099 ms |
| 전체 접기 | 43 ms | 52 ms | 530 ms |
| 폴더 1개 갱신 | 1.8 ms | 2.5 ms | 30 ms |
| 이름 전수 매칭 (1회) | 0.4 ms | 1.0 ms | 8.8 ms |
| 폴드 버튼 판정 (20회) | 0.1 ms | 0.1 ms | 0.2 ms |

### 읽는 법

**첫 줄이 핵심이다.** 시작 시 실제로 그려지는 것은 최상위 행뿐이므로
**볼트 크기와 무관하게 11–19ms**다. 접힌 폴더가 DOM을 만들지 않는 설계가 의도대로
동작한다. 지시서 §14의 "수천 개 파일" 요구는 **충족**.

걱정했던 폴드 버튼 순회는 `some()` 조기 종료 덕에 사실상 공짜다. O(N²) 아니었음.

전체 펼치기만 문제다. 행당 비용은 0.15–0.3ms로 대체로 평평하니 알고리즘 문제는
아니고, 20,000개에서 상수가 나빠지는 건 jsdom의 DOM 비용과 GC 압박으로 보인다.
**jsdom은 브라우저보다 훨씬 느리므로 이 숫자는 상한선이지 예측값이 아니다.**

### 실기기 확인 결과

사용자가 실제 Obsidian에서 5,000개 볼트를 열어봤다. **"펼치기가 다소 느리지만 할 만하다,
문제없다."** → 전체 펼치기에 경고나 확인 대화상자는 **넣지 않는다.** jsdom 숫자가
상한선이라는 예상이 맞았다.

### 검색에 주는 함의

이름 전수 매칭이 20,000개에서 8.8ms(jsdom)다. 브라우저라면 1–2ms. **타이핑마다
전체를 훑어도 된다**는 뜻 — 인덱스를 따로 만들 필요가 없다. 단, 결과 트리를
다시 그리는 비용이 큰 쪽이므로 디바운스(~150ms)와 결과 수 제한을 둔다.

## 2026-09-06 — 첫 실행에 뷰 열기, 시연용 볼트

### 설치했는데 아무것도 안 보이는 문제

`bench-vault`에서 "TreeNav가 안 보인다"는 보고. 조사해보니 설치는 정상이었고
(플러그인이 `data.json`을 썼고 리본 항목도 `hiddenItems`에 `false`로 등록돼 있었다)
**뷰 leaf만 없었다.** 리본 아이콘을 찾아 눌러야 한다는 걸 몰랐던 것.

이건 공개 후 **모든 신규 사용자가 겪을 문제**다. 그래서 **볼트에서 처음 로드될 때
(`data.json`이 없을 때) 한 번만** `onLayoutReady`로 뷰를 연다. 기존 사용자는 저장된
상태가 있으므로 레이아웃이 건드려지지 않는다 — 스모크에서 그 조건을 지킨다.

### 시연용 볼트 (`npm run demo`)

주제는 **우주·천문**, 영어. 노트 35개 + 빈 폴더 1개.

볼트만 만드는 게 아니라 **`data.json`과 `workspace.json`까지 같이 쓴다**:

- 이모지 아이콘 16종, 색, 형광펜 2곳, 굵게/기울임
- 수동 정렬(알파벳순이 아님이 한눈에 보이게)
- **`Astrophysics/Neutron stars`를 `nestedFolders`에 넣어** — 노트가 자식을 품어
  폴더가 된 상태를 시연. 그 노트 본문에도 그렇게 적어뒀다
- 클래식 트리 기본
- 왼쪽 사이드바에 TreeNav가 열린 채로, 가운데에 노트 하나가 열린 레이아웃

볼트가 `data.json`을 갖고 시작하므로 위의 "첫 실행 자동 열기"가 적용되지 않는다.
그래서 `workspace.json`을 직접 써준다 — 스크린샷이 "패널 여는 걸 기억하는가"에
의존하면 안 된다.

생성기는 커밋하고 볼트는 gitignore. 누구나 같은 화면을 재현할 수 있다.

## 2026-09-06 (2) — 검색 + 필터

### 팝업이지 인라인 필터가 아니다

처음엔 "검색어에 맞는 항목만 남기고 트리를 접는" 인라인 방식을 제안했는데,
사용자가 **팝업 + 하위 필터 + 결과 클릭 시 트리 이동**을 제시했고 그쪽이 맞았다.

- 사이드바는 300px다. 필터 네 개를 그 폭에 넣으면 트리 볼 자리가 없다
- 팝업은 **트리를 다시 그리지 않는다.** 측정에서 비싼 쪽이 재렌더였다
- 퀵 스위처와 같은 관용구라 배울 게 없다

못 하는 일은 "걸러진 트리 안에서 작업하기"다. 매칭 로직(`SearchService`)은 UI와
분리돼 있으니, 필요해지면 인라인 모드를 얹으면 된다.

### 필터는 전부 옵시디언 자신의 데이터

종류 / 확장자 / 수정일 / **태그**. 한때 "TreeNav로 표시해둔 것"(형광펜·색)을 넣을까
했지만 **뺐다.** 아이콘과 색은 *보기 좋으라고* 고르는 것이지 *표시하려고* 고르는 게
아니다. 그걸 필터 기준으로 삼으면 남의 장식을 결정으로 읽는 셈이 된다.
표시 개념이 필요하면 나중에 Pin을 만드는 게 정직하다.

- `file.stat.mtime` — 공개 API
- `getAllTags(cache)` — 공개 API, 인라인 태그와 프론트매터 태그를 **둘 다** 처리
- 부모 태그는 하위를 포함한다 (`#project`가 `#project/alpha`를 잡는다) — 옵시디언
  태그 패널과 같은 규칙
- **볼트의 전체 태그 목록을 주는 공개 함수는 없어서** 파일별 캐시를 훑어 모은다

### 폴더는 결과에 없다

대부분의 폴더에는 폴더 노트가 있고, 그 노트를 고르면 **트리에서는 폴더 행에 도착한다**
(`revealActive`가 숨겨진 폴더 노트를 폴더로 매핑하므로). 그래서 폴더를 따로 넣지 않았다.
빈틈: **폴더 노트가 없는 폴더는 찾을 수 없다.**

### 퍼지 매칭이 아니다

부분 일치(대소문자 무시)에 순위만 매긴다: 이름으로 시작 → 이름에 포함 → 경로에 포함.
이미 보고 있는 트리를 좁히는 게 목적이라, 퍼지는 다른 질문에 답하면서 노이즈만 늘린다.

### 인덱스를 만들지 않았다

타이핑마다 `vault.getFiles()` 전체를 훑는다. 20,000개에서 8.8ms(jsdom)라 그래도 된다.
인덱스는 볼트와 어긋나기 시작하면 고치기 어려운 종류의 것이다.

### `SuggestModal` 안에 필터 끼워넣기

필터를 바꿨을 때 검색을 다시 돌리는 **공개 호출이 없다.** `inputEl`에 `input` 이벤트를
직접 쏜다 — 서제스터가 실제로 듣고 있는 것이 그거다. DOM 이벤트라 문서화되지 않은
멤버를 건드리는 것보다 안전하다.

하네스에도 `SuggestModal` 스텁을 제대로 만들어서(`inputEl` / `resultContainerEl` /
`runQuery`), 대화상자를 열고 → 타이핑하고 → 결과를 고르고 → **트리가 그 행에 도착했는지**
까지 검증한다.

## 2026-09-06 (3) — 낡은 번들을 읽고 있었다

`Copy path`가 `Delete` 뒤에 붙는 걸 고치려고 섹션 이름 `"clipboard"`를 목록에 넣었는데
**아무 변화가 없었다.** 사용자가 "그대로인 듯"이라고 알려줘서 다시 봤다.

원인: **읽던 asar이 실제 도는 버전이 아니었다.**

```
Program Files/Obsidian/resources/obsidian.asar   2025-04  ← 설치본(구버전), 내가 읽던 것
%APPDATA%/obsidian/obsidian-1.12.7.asar          실제 실행 중  ← 자동 업데이트본
```

Obsidian은 업데이트를 AppData에 새 asar로 받는다. 설치 폴더의 것은 한참 전에 멈춰 있다.
스크린샷 타이틀바에 `Obsidian 1.12.7`이라고 쓰여 있었는데 확인하지 않았다.

그 사이 섹션 이름이 바뀌었다:

```
1.8.x    setSection("clipboard")
1.12.7   setSectionSubmenu("info.copy", { title: copyPath() })   ← 섹션 전체가 서브메뉴
```

1.12.7의 파일 탐색기 정렬 목록:

```js
["title","open","action-primary","action","info","info.copy","view","system","","danger"]
```

그대로 쓰도록 고쳤다. **목록에 없는 섹션은 맨 뒤로 밀린다** — 그게 Copy path가 Delete
뒤에 있던 이유다. 앞으로 어떤 그룹이 바닥에 나타나면 이 목록부터 본다.

### 배운 것

이전 조사(leaf/source)는 결과적으로 맞았다 — 사용자 스크린샷에서 로컬 그래프 5종이
사라지고 버전 내역이 나타난 것으로 실증됐다. **구버전을 읽고도 답이 맞아떨어져서
문제를 눈치채기 어려웠다.** 다음부터 asar을 뒤질 일이 있으면 `%APPDATA%`부터 본다.

24MB 단일 라인 번들이라 긴 lookbehind 정규식은 타임아웃한다. `grep -a -b -o`로
바이트 오프셋을 얻고 `dd`로 창을 떠서 읽는 게 확실하다.

## 2026-09-06 (4) — 1.0.0, 그리고 네이티브 메뉴

### 버전을 1.0.0으로

기능이 지시서를 다 덮었다고 판단. `manifest.json` · `package.json` · `versions.json`
세 곳. `versions.json`은 옛 항목을 남겨둔다 — 구버전 Obsidian도 설치 가능한 버전을
안내받아야 한다.

```json
{ "0.1.0": "1.5.0", "1.0.0": "1.8.7" }
```

`0.1.0`이 1.5.0인 것은 그때 `Vault.copy`를 쓰지 않았기 때문이고, 지금 하한은 그것 때문에
1.8.7이다. **검증된 하한**이라는 점이 이전(근거 없이 적힌 1.5.0)과 다르다.

### 스크린샷

사용자가 4장을 찍어 `docs/screenshots/`에 넣었고 3장을 README에 썼다. 아이콘 선택기는
뺐다 — Font & color 대화상자가 이미 외형을 대표하고, 전체 창 이미지 넷이 연달아 나오면
설명이 아니라 광고로 읽힌다.

**스크린샷이 버그를 하나 잡아줬다.** sc1에서 `Copy path`가 `Delete` 뒤에 있었다.
(원인과 수정은 바로 위 항목.)

### 네이티브 메뉴 — 우리 문제가 아니었던 것

실사용 볼트에서 "메뉴 간격이 왜 이렇게 넓냐"는 질문. 원인은 **설정 → 모양 → Native menus**
가 켜져 있던 것.

> "Menus throughout the app will match the operating system.
>  They will not be affected by your theme."

켜면 Windows가 메뉴를 그리므로 **아이콘이 전부 사라지고**(OS 메뉴는 SVG를 못 그린다)
간격이 OS 기준이 된다. 우리 항목만이 아니라 코어 항목도 마찬가지다. 볼트별 설정이라
데모 볼트(꺼짐)와 실사용 볼트(켜짐)가 달라 보였다.

같은 이유로 **폰트가 다르다**는 질문도 있었는데, 그건 그 볼트가 쓰는 **Things 테마**가
`--font-interface-theme`를 재정의하기 때문. TreeNav는 자체 font-family를 지정하지 않고
테마를 따라간다 — 의도한 동작이고 코어 탐색기와 같다.

**둘 다 코드 변경 없음.** 사용자 환경 차이를 우리 버그로 오인하지 않으려면 먼저
`appearance.json`을 봐야 한다는 걸 기록해 둔다.

### 배포 상태

- GitHub: <https://github.com/hyrocket/treenav> — main 최신
- 실사용 볼트 `D:\Obsidian_Files\Obsidian_HY`에 설치, 실사용 테스트 시작
- **태그와 릴리스는 아직 만들지 않았다.** 실사용에서 고칠 게 나오면 1.0.0을 다시 만들어야
  하므로, 테스트가 끝난 뒤에 태그 → 릴리스(main.js·manifest.json·styles.css 첨부) 순서로 간다

### 남은 작업

**공개 전에 해야 하는 것**

1. **실사용 테스트** — 진행 중. 걸리는 것을 모아 고친 뒤 다음으로 넘어간다.
2. **태그 + GitHub 릴리스** — 태그는 `v` 없이 `1.0.0`. main.js · manifest.json ·
   styles.css 세 파일을 릴리스에 첨부해야 심사를 통과한다.
3. **커뮤니티 플러그인 등록 PR** — `obsidianmd/obsidian-releases`. `gh` CLI가 아직 없다.

**실기기 확인이 남은 것**

- 모바일 드래그는 하네스(jsdom)에서만 검증했다. 실제 폰에서 길게 누르기 타이밍(400ms),
  스크롤과의 충돌, 고스트 위치를 한 번 봐야 한다. 데스크톱은 실사용으로 확인됨.

**Phase 3 잔여**

- 특정 파일·폴더 숨기기
- Favorite / Pin (필터의 "핀만 보기"로 이어짐)

(검색 / 필터, Manual Sorting, Tree 표시 옵션은 완료)

**그 외 빈틈**

- 키보드 타입어헤드
- 폴더 노트가 없는 폴더는 검색되지 않음
- 아이콘 선택기 다단에서 위/아래 화살표가 격자 감각과 어긋남
- 트리 전체 글꼴 설정은 없음 (항목별로만). 사용자가 "필요 없다"고 판단
- ESLint 설정 없음

**완료된 것 (이전 목록에서 지움)**

- 대용량 성능 측정 → 접힌 렌더는 볼트 크기와 무관하게 11–19ms. 5,000개 실사용 확인
- 활성 노트로 자동 스크롤 → 커맨드 + 설정(기본 꺼짐)으로 구현
- 모바일 드래그 → 포인터 이벤트로 재작성

### 재개 방법

```bash
npm install
npm run reset          # test-vault를 알려진 fixture로 재생성 (git 미추적)
npm run dev            # 감시 빌드 + test-vault 자동 배포
npm run build          # 타입체크 + 번들 + 스모크 테스트
npm run smoke -- --vault test-vault --data test-vault/.obsidian/plugins/treenav/data.json
```

Obsidian에서 `D:\dev_projects\006_treenav_obsidian_plugin\test-vault`를 vault로
열고, 코드 수정 후 `Ctrl+P` → **Reload app without saving**.

실제 vault는 `D:\Obsidian_Files\Obsidian_HY` — `notebook-navigator`,
`manual-sorting`, `file-explorer-note-count`가 이미 설치돼 있어 탐색기 영역이
겹친다. 기능 검증은 test-vault에서 하는 편이 정확하다.

### 저장소

<https://github.com/hyrocket/treenav> · GPL-3.0-or-later

MIT에서 GPL로 바꾼 이유: MIT는 소스를 닫고 재배포하는 것을 막지 못한다. GPL은
배포되는 파생물이 GPL을 유지하고 전체 소스를 공개하도록 강제한다. 포크 자체를
막는 라이선스는 없으므로, 이름은 상표 영역으로 분리해 README에 "배포하는 포크는
개명할 것"을 명시했다.
